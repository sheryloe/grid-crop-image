# 서비스 제품 고도화 제안서

## 1) 서비스 제품화를 위한 고도화 기능 식별

### A. 코어 기능
- **프로젝트 단위 관리**: 이미지 세트/설정/출력 폴더를 프로젝트로 저장·복원.
- **작업 템플릿 라이브러리**: 자주 쓰는 크롭 레이아웃(블로그, 쇼핑몰, SNS)을 템플릿으로 제공.
- **이력/감사 로그**: 언제 어떤 설정으로 몇 장이 생성됐는지 추적 가능.
- **실패 복구**: 배치 도중 에러 발생 파일만 재시도.

### B. 운영/품질
- **자동 업데이트 채널**: 안정판/베타판 분리 배포.
- **원격 진단 로그(옵트인)**: 장애 분석 및 품질 개선.
- **설정 백업/복구**: 사용자 PC 교체 시 설정 이전.

### C. 수익화/확장
- **Pro 기능 게이팅**: OCR, API 자동화, 팀 공유 등 유료 기능 분리.
- **플러그인 구조**: OCR 엔진, 후처리 필터, 저장소(S3/Drive) 확장.

## 2) 고도화 기능 자동화 방향

- **자동 레이아웃 추천**: 이미지 해상도와 종횡비를 기반으로 추천 그리드 제시.
- **자동 파일명 규칙**: 날짜/프로젝트명/순번 규칙을 사용자 정의 템플릿으로 자동 적용.
- **자동 품질 점검**: 너무 작은 크롭, 중복 크롭, 빈 영역을 사전에 탐지.
- **자동 이력 축적**: 이번 구현처럼 분할/배치 결과를 JSONL로 누적 저장하고 뷰어에서 즉시 확인.

## 3) 설치 파일 + 이력 Viewer 전략

### 이번 코드 반영 사항
- PyInstaller 기반 설치 파일(EXE) 구조 유지.
- 메인 툴바에 **이력 뷰어** 버튼 추가.
- 단일 분할/배치 처리 완료 시 `.crop_history.jsonl` 파일에 작업 이력 자동 기록.
- 이력 뷰어에서 최신 이력을 선택해 JSON 상세 확인 가능.

### 배포 시 권장
- 설치본에 기본 데이터 경로를 `%LOCALAPPDATA%/AutoCropSplitter`로 고정.
- 이력 파일 롤링(예: 10MB 단위 분할) 및 내보내기(CSV/JSON) 기능 추가.

## 4) OCR 버전 + TensorRT/모바일 검토

### 구현 가능성 결론
- **가능**. 다만 데스크톱/모바일을 분리 설계하는 것이 현실적.

### 데스크톱 OCR(1차)
- 후보: PaddleOCR, Tesseract, EasyOCR.
- 흐름: `크롭 -> OCR -> 텍스트/좌표 저장(JSON)`.
- 결과물: 검색 가능한 크롭 히스토리(텍스트 포함).

### 고성능 추론(2차)
- TensorRT는 **NVIDIA GPU가 있는 Windows 장비**에서 가속 효과가 큼.
- ONNX 변환 + TensorRT 엔진 빌드 파이프라인 필요.
- 설치 복잡도(CUDA/cuDNN 버전 호환)를 줄이기 위해 별도 `GPU Edition` 배포 권장.

### 모바일 OCR(3차)
- Android/iOS는 TensorRT 직접 적용보다 **ONNX Runtime Mobile / TFLite / CoreML**이 일반적으로 유리.
- 모바일 앱에서는
  - 경량 OCR 모델
  - 오프라인 추론
  - 결과 동기화(API)
  구조 권장.

### 추천 로드맵
1. Desktop OCR PoC(정확도/속도 벤치마크)
2. OCR 결과를 이력 뷰어에 통합(검색/필터)
3. GPU 가속 에디션(TensorRT) 분기
4. 모바일 경량 OCR 앱/SDK 연동

## 5) 이번 스프린트 착수(개발 시작)

- Python UI에서 C++ OCR 실행기 호출 경로를 우선 연결.
- `cpp/ocr_engine`에 CMake 기반 C++17 프로젝트를 추가해 TensorRT 적용 포인트를 코드에 명시.
- 현재는 placeholder OCR 결과를 반환하지만, 엔진 로드/추론부만 교체하면 즉시 고성능 경로로 확장 가능.

## 6) 앞으로 이어서 추가할 항목 식별 (우선순위 백로그)

### P0 (바로 다음 스프린트)
- **실제 TensorRT 추론 구현 완료**
  - `detector.engine`/`recognizer.engine` deserialize, context 생성, enqueue, 전/후처리 연결.
  - 종료 기준: placeholder 제거 후 샘플 이미지에서 실제 텍스트+confidence 산출.
- **OCR 이력 고도화**
  - 현재 preview 20개 제한 대신 전체 결과 저장 + UI에서는 페이지네이션/지연 로딩.
  - 종료 기준: 대량 OCR(예: 500 crop)에서도 이력 뷰어 프리징 없이 탐색 가능.
- **탭별 UX 최적화**
  - `이력 목록` 탭: 정렬(최신/오래된/저장 개수), 빠른 프리셋 필터(오늘/7일/30일).
  - `OCR 결과` 탭: confidence 임계값 슬라이더 + 낮은 신뢰도 강조 표시.
  - 종료 기준: 3번 클릭 이내로 원하는 작업 로그 탐색 가능.

### P1 (제품 안정화)
- **이력 저장소 안정화**
  - JSONL 롤링(파일 크기 단위), 손상 라인 자동 스킵/복구, 백업 파일 생성.
  - 종료 기준: 장기 사용 시에도 단일 이력 파일 비대화/손상에 대한 복원성 확보.
- **배치 실패 재시도 워크플로우**
  - 실패 건만 재실행, 실패 원인 코드화(파일 손상/권한/모델 없음 등).
  - 종료 기준: 배치 실패 후 “실패 항목 재시도” 한 번으로 재처리 가능.
- **설치본 품질**
  - EXE 설치/업데이트/제거 시 사용자 데이터(이력/설정) 보존 정책 명확화.
  - 종료 기준: 신규/업데이트/제거 시나리오 체크리스트 100% 통과.

### P2 (확장)
- **모바일 경량 OCR 경로**
  - ONNX Runtime Mobile/TFLite 후보 비교 벤치마크 및 공통 결과 스키마 설계.
  - 종료 기준: 모바일 샘플 앱에서 동일 포맷(JSON)으로 OCR 결과 업로드/조회 가능.
- **팀 협업 기능**
  - 프로젝트 단위 공유, 역할 기반 접근, 히스토리 내보내기 API.
  - 종료 기준: 2인 이상 환경에서 충돌 없이 이력 추적 가능.

### 기술 부채/리스크 메모
- TensorRT 런타임·CUDA 버전 호환성 매트릭스 문서화가 필요함.
- OCR 결과 스키마를 고정(`version` 필드 포함)하지 않으면 이후 마이그레이션 비용이 커짐.
- Tkinter UI는 대용량 로그 렌더링에서 병목 가능성이 있어 가상 리스트/지연 렌더링 도입 검토 필요.
