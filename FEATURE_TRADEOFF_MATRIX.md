# 기능 고도화 트레이드오프 매트릭스 (현재 기준)

목표: **기능 10개**를 식별하고, 성능/무게(앱 반응속도, 메모리, 배포 용량, 개발 복잡도) 기준으로
"넣을지/보류할지" 판단한다.

## 평가 기준
- 성능 영향(런타임): 낮음(1) ~ 높음(5)
- 메모리/디스크 영향: 낮음(1) ~ 높음(5)
- 개발/운영 복잡도: 낮음(1) ~ 높음(5)
- 사용자 가치: 낮음(1) ~ 높음(5)
- **총 비용 점수** = 성능 + 메모리 + 복잡도 (낮을수록 유리)
- **우선 적용 지수** = 사용자 가치 - (총 비용 / 3)

> 참고: 정량 수치는 초기 추정치이며, 실제 측정(프로파일링/실사용 데이터)으로 보정 필요.

## 10개 기능 식별 + 트레이드오프

| # | 기능 | 사용자 가치 | 성능 영향 | 메모리/디스크 | 복잡도 | 총 비용 | 우선 적용 지수 | 판단 |
|---|---|---:|---:|---:|---:|---:|---:|---|
| 1 | 이력 뷰어 가상 리스트(지연 렌더링) | 5 | 1 | 1 | 2 | 4 | 3.7 | **즉시 추가** |
| 2 | OCR 결과 서버치 전체 저장 + 페이지네이션 | 4 | 2 | 3 | 3 | 8 | 1.3 | **조건부 추가** |
| 3 | 이력 검색 인덱스(날짜/작업유형/텍스트) | 4 | 1 | 2 | 3 | 6 | 2.0 | **추가 권장** |
| 4 | 썸네일 미리보기 캐시 | 3 | 2 | 3 | 2 | 7 | 0.7 | **옵션화 후 추가** |
| 5 | 배치 실패 건 재시도 큐 | 5 | 1 | 1 | 2 | 4 | 3.7 | **즉시 추가** |
| 6 | 자동 업데이트(안정/베타 채널) | 4 | 1 | 2 | 4 | 7 | 1.7 | **중기 추가** |
| 7 | C++ TensorRT 실제 추론 경로 완성 | 5 | 2 | 2 | 5 | 9 | 2.0 | **핵심 추가** |
| 8 | GPU/CPU 자동 폴백 + 안전 모드 | 5 | 1 | 1 | 3 | 5 | 3.3 | **즉시 추가** |
| 9 | 원격 진단 로그(옵트인) | 3 | 1 | 1 | 4 | 6 | 1.0 | **보안정책 후 추가** |
| 10 | 모바일 연동 API + 동기화 | 4 | 2 | 2 | 5 | 9 | 1.0 | **후순위** |

## 포함/제외 기준 (무거우면 빼기)

### 포함 추천 (현재 바로 넣어도 무겁지 않음)
1. 이력 뷰어 가상 리스트(지연 렌더링)
2. 배치 실패 재시도 큐
3. GPU/CPU 자동 폴백 + 안전 모드
4. 이력 검색 인덱스

이 4개는 공통적으로 런타임/메모리 비용이 낮고 UX 개선 폭이 큼.

### 조건부 포함 (옵션 플래그/설정으로)
5. OCR 전체 저장 + 페이지네이션
6. 썸네일 캐시
7. 자동 업데이트 채널

이 3개는 사용자 가치는 높지만 운영 복잡도 또는 디스크 사용량 증가가 있어 **옵션화**가 적합.

### 후순위/검증 후 포함
8. 원격 진단 로그(옵트인 + 데이터 정책)
9. 모바일 연동 API

보안/정책/운영 체계 정리 없이는 도입 리스크가 큼.

### 반드시 진행 (핵심 기능)
10. C++ TensorRT 실제 추론 경로 완성

핵심 제품 방향과 직접 연결되므로 제외 대상이 아니라 **성능 측정 기반으로 단계 도입**해야 함.

## 무게 증가를 막는 구현 원칙
- 기본값은 경량 모드(썸네일 캐시/원격로그/자동업데이트는 OFF).
- OCR 결과 이력은 원본 전체를 저장하되, UI는 지연 로딩.
- 이력 파일은 롤링(예: 10MB) + 인덱스 파일 분리.
- GPU 기능은 하드웨어 체크 후 자동 폴백(실패 시 CPU/placeholder).
- 모든 신규 기능은 "측정 후 유지" 원칙 적용:
  - 시작시간
  - OCR 100개 처리 시간
  - 메모리 피크
  - 앱 idle 메모리

## 바로 실행 가능한 다음 액션 (2주)
1. 배치 실패 재시도 큐 구현
2. 이력 리스트 가상화 + 검색 인덱스 추가
3. GPU/CPU 폴백 상태 UI 표시
4. TensorRT 실추론 최소 경로(det/rec inference) 연결

